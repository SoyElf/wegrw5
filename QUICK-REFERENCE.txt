┌─────────────────────────────────────────────────────────────────────────────┐
│                   COPILOT CLI MODEL NAMES - QUICK REFERENCE                 │
│                          Copilot v1.0.12 - Complete                         │
└─────────────────────────────────────────────────────────────────────────────┘

COMMAND FORMAT:
  copilot -p "<prompt>" --model <MODEL_NAME> --allow-all
  ─────────────────────────────────────────────────────────
  
RECOMMENDED MODELS:
  ✓ claude-opus-4.6              Default, most capable (recommended)
  ✓ claude-sonnet-4.6            Balanced, faster alternative
  ✓ claude-haiku-4.5             Lightweight, fastest
  ✓ gpt-5.4                      Latest OpenAI version

FULL MODEL LIST (20 total):

  CLAUDE (8 models):
    claude-haiku-4.5
    claude-opus-4.5, claude-opus-4.6, claude-opus-4.6-fast, claude-opus-4.6-1m
    claude-sonnet-4, claude-sonnet-4.5, claude-sonnet-4.6

  GPT (11 models):
    gpt-4.1, gpt-5-mini
    gpt-5.1, gpt-5.1-codex, gpt-5.1-codex-mini, gpt-5.1-codex-max
    gpt-5.2, gpt-5.2-codex, gpt-5.3-codex, gpt-5.4, gpt-5.4-mini

  GEMINI (1 model):
    gemini-3-pro-preview

FORMAT RULES:
  ✓ Use hyphens:        claude-opus-4.6
  ✓ Use lowercase:      gpt-5.4
  ✓ Semantic version:   4.6, 5.2, 5.4
  ✗ NO underscores:     claude_opus_4_6 ← WRONG
  ✗ NO CamelCase:       Claude-Opus-4.6 ← WRONG

EXAMPLE COMMANDS:

  # Default model
  $ copilot -p "What is Docker?" --model claude-opus-4.6 --allow-all

  # Lightweight model
  $ copilot -p "Fix the bug" --model claude-haiku-4.5 --allow-all

  # OpenAI option
  $ copilot -p "Analyze code" --model gpt-5.4 --allow-all

  # Long context
  $ copilot -p "Summarize this 50-page doc" --model claude-opus-4.6-1m --allow-all

  # Interactive mode
  $ copilot -i "Help me debug" --model claude-opus-4.6

ERROR MESSAGES:

  Invalid model:
    $ copilot -p test --model gpt-4 --allow-all
    Error: Model "gpt-4" from --model flag is not available.

  Valid model (will work):
    $ copilot -p test --model gpt-5.4 --allow-all
    (waits for API response...)

WHAT ARTICLES GET WRONG:

  Article says                Actual CLI name          Comment
  ─────────────────────────────────────────────────────────────────
  claude-3-5-haiku      ≠      claude-haiku-4.5        Version changed
  gpt-4                 ≠      gpt-4.1 or gpt-5.4      GPT-4 not available
  claude-3-sonnet       ≠      claude-sonnet-4.6       Old naming

DEFAULT CONFIGURATION:
  File: ~/.copilot/config.json
  Content: "model": "claude-opus-4.6"
  
  To change default, edit config.json:
    { "model": "claude-opus-4.6" }

VERIFICATION:

  Known working models (all listed above are tested and available)
  
  Invalid model → Returns error immediately:
    Error: Model "X" from --model flag is not available.

  Valid model → Command starts (might timeout on network):
    (no error message, proceeds to API call)

OTHER USEFUL FLAGS:

  --allow-all              Grant all permissions (batch mode)
  --effort high           Set reasoning effort (low|medium|high|xhigh)
  --share                 Share session to markdown file
  --share-gist            Share session to secret GitHub gist
  --silent                Output only agent response
  -i "prompt"             Interactive mode with initial prompt

WRAPPER SCRIPT EXAMPLE:

  #!/bin/bash
  MODEL="${1:-claude-opus-4.6}"
  PROMPT="${2:-Your prompt here}"
  copilot -p "$PROMPT" --model "$MODEL" --allow-all

VALIDATION CHECKLIST:

  Use this to verify model name is correct:
  ☐ Model name uses hyphens only
  ☐ Model name is all lowercase
  ☐ Model name follows existing pattern
  ☐ Model name exists in this reference
  ☐ Command format: copilot -p "..." --model <name> --allow-all

═════════════════════════════════════════════════════════════════════════════
For detailed info, see: INDEX.md, MODEL-DISCOVERY-REPORT.md, or other docs
═════════════════════════════════════════════════════════════════════════════
